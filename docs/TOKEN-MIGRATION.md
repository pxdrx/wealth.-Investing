# TOKEN-MIGRATION

> Owner: Track A (brand). Other tracks read-only.

TODO(track-a): document migration of design tokens (tailwind.config.ts, globals.css)
to the consolidated brand system. Capture:
- old token name → new token name
- affected files
- migration date
- breaking changes
