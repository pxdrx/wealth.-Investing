// TODO(track-c): canonical entitlements API.
// Reuse Plan type from ./subscription ("free" | "pro" | "ultra" | "mentor").
// Expose: hasAccess(plan: Plan, feature: Feature): boolean.
// Consumed by Track A (brand UI gating) and Track B (landing CTA gating).
export {};
