// TODO(track-b): canonical analytics event names + track() helper.
// Consumed by Track A (brand interactions) and Track C (app events).
// Define event union type + track(event, payload) function.
export {};
